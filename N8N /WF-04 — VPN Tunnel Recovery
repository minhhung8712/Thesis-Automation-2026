[Trigger từ WF-00]
→ VPN tunnel [Site X] down
        │
        ▼
[Xác nhận sự cố]
→ Ping test đến Site X: 3 lần / 30 giây
→ Nếu cả 3 fail → xác nhận tunnel thật sự down
        │
        ▼
[Phân loại nguyên nhân]
→ pfSense log: timeout / auth fail / config error?
        │
        ├─[Timeout / Flap]──→ Auto restart tunnel
        │                     Ansible: restart_vpn.yml
        │                     Kiểm tra lại sau 2 phút
        │                     Nếu vẫn fail → leo thang
        │
        └─[Auth fail / Config]──→ KHÔNG auto-fix
                                   → [WF-SUB-01: Human Approval]
                                   → Gửi alert ngay cho admin
