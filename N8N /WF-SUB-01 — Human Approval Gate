[Nhận request từ WF cha]
→ Payload: {incident_type, severity, 
            src_ip, action_proposed, 
            ansible_playbook}
        │
        ▼
[Gửi Telegram với Inline Button]
→ Message:
  "🚨 [SEVERITY] Incident #{ID}
   Loại: {incident_type}
   IP: {src_ip} | Site: {site}
   Hành động đề xuất: {action}
   ⏱ Timeout: 15 phút"
  
  [✅ APPROVE] [❌ REJECT] [🔍 XEM THÊM]
        │
        ▼
[Webhook chờ callback từ Telegram Bot]
        │
        ├─[APPROVE]──→ WF-SUB-02: Ansible Execute
        │
        ├─[REJECT]───→ Log lý do + WF-SUB-03: Notify
        │
        ├─[XEM THÊM]─→ Gửi log chi tiết cho admin
        │               Tiếp tục chờ
        │
        └─[TIMEOUT 15 phút]──→ Auto-escalate
                               → Gửi alert lên cấp trên
                               → Log "No response — pending"
